import React from 'react'

import { Wrapper, DualRing } from './styles'

const LoadingSpinner = (props) => {
  return (
    <Wrapper>
      <DualRing />
    </Wrapper>
  )
}

export default LoadingSpinner
